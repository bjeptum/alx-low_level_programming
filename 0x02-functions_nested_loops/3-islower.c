#include "main.h"
/**
 * _islower - checks for lowercase character
 * c: the character to be inputted
 *
 * Return: 0 (Exit)
 */
int _islower(void)
{
	int c;

	if ((_islower(c))
			{
			_putchar(1);
			else
			_putchar(0);
			}
			return (0);
}
