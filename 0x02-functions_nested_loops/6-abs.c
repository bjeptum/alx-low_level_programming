#include "main.h"

/**
 * _abs - prints the absolute value of an integer
 * @i - integer to be inputted
 * 
 * Return: interger
 */
int _abs(int i)
{
	if (i > 0)
	{
		return (i);
	}
	else if (i < 0)
	{
		return (-i);
	}
	else
	{
		return (i)
	}
}
