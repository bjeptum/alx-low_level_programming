#include "main.h"

/**
 * print_last_digit - prints the last digit of a number
 * @num: digit to be inputted
 *
 * Return: 0 (exit)
 */
int print_last_digit(int);
{
	int num, digit;

	digit = num % 10;
	_putchar (digit);
	return (0);
}
