#include "main.h"

/**
 * print_last_digit - prints the last digit of a number
 * @int: integer to be inputted
 *
 * Return: 0 (exit)
 */
int print_last_digit(int);
{
	int digit;

	digit = int  % 10;
	_putchar (digit);
	return (0);
}
