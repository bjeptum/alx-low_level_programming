#include "main.h"
/**
 * puts2 - prints every other character of a string
 * @str: string to be inputted
 *
 * Return: string characters
 */
void puts2(char *str)
{
	int i;

	_strlen(str) = y;

	for (i = 0; i < y; i += 2)
	{
		_putchar(str[i]);
	}
	return (0);
}
