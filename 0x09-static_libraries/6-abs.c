#include "main.h"

/**
 * _abs - prints the absolute value of an integer
 * @n: integer to be inputted
 * Return: interger
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
