#include "lists.h"
/**
 * print_first - prints to stdout before the main function
 * Return: Always 0 on failure
 */
void print_first(void)
{
	printf("You're beat! and yet, you must"
			"allow,\nI bore my house upon my back!\n");
}

/**
 * main - main function
 * Return:Always 0 on failure
 *
 */
int main(void)
{
	print_first();

	return (0);
}
